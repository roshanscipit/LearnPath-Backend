# =============================================
# SERVER
# =============================================
server.port=8183

# =============================================
# POSTGRESQL DATABASE (Render)
# =============================================
spring.datasource.url=jdbc:postgresql://dpg-d9egj03bc2fs73807il0-a/doliuw_db?sslmode=require
spring.datasource.username=doliuw_db_user
spring.datasource.password=IdCApFrNa47RwdEL5UKiKr14GlPCB1Lu
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# =============================================
# JPA / HIBERNATE
# =============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# =============================================
# CAFFEINE CACHE
# =============================================
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=10m
spring.cache.cache-names=companies,roles,users,userProgress,mockTests,bookings


google.oauth.client-id=40523331635-g9shj2fpg527lhkhrj2pasbnicqcrctn.apps.googleusercontent.com

# =============================================
# JWT
# =============================================
app.jwt.secret=doliuwSecretKey2025SuperSecureBase64EncodedKeyForHMACSHA256Algorithm
app.jwt.expiration-ms=86400000
# 24 hours = 86400000 ms

# =============================================
# OTP (Demo – replace with Twilio in prod)
# =============================================
app.otp.expiry-minutes=10
app.otp.demo-mode=true

# =============================================
# CORS
# =============================================
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001

# =============================================
# LOGGING
# =============================================
logging.level.com.doliuw=DEBUG
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN

# =============================================
# EMAIL (Gmail SMTP – update with your credentials)
# =============================================
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=satputenandkishor36@gmail.com
spring.mail.password=xsmtpsib-01b81c933e0bc4d5d679a7a3e02a575ea52bd27e5b46d2db08c8f54e9a7e3af1-G1yNN889JcY4gUhW
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Admin email that receives booking notifications
app.admin.email=admin@doliuw.com

# Enable async for non-blocking email dispatch
spring.task.execution.pool.core-size=2
spring.task.execution.pool.max-size=5

# =============================================
# FIELD-LEVEL ENCRYPTION (AES-256-GCM)
# =============================================
# 32-byte (256-bit) key encoded as Base64
# In production: set via env var APP_ENCRYPTION_SECRET
# Generate new key: openssl rand -base64 32
app.encryption.secret=0Qn71rmPyo1b8xFx8Jw0jEBCZiHJo54n9Arp+2plw3k=